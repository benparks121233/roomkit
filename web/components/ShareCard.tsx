// web/components/ShareCard.tsx
// Clean, screenshot-friendly card for social sharing.
// Shows: room render, top 3 products (name + price), total, RoomKit branding.
// This is the viral unit — every shared board is an ad with affiliate links embedded.
// Stage 9: implement.

export default function ShareCard({ runId }: { runId: string }) {
  // Stage 9: render + top products + total + branding
  return <div>ShareCard for run {runId} — Stage 9</div>;
}
