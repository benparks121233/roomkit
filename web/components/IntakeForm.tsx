// web/components/IntakeForm.tsx
// Room type selector + dimensions input + optional photo upload.
// Stage 9: implement. Keep fields minimal — "12×14 bedroom, $1,500, warm-minimalist"
// beats fragile blueprint OCR. All data stays in React state; no localStorage.

export default function IntakeForm() {
  // Stage 9: room_type, dimensions, photo_url (optional)
  return <div>IntakeForm — Stage 9</div>;
}
