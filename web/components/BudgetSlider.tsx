// web/components/BudgetSlider.tsx
// Budget input (slider + text field).
// Validated client-side (> 0) before submission; server re-validates.
// Stage 9: implement.

export default function BudgetSlider() {
  // Stage 9: budget (number, USD)
  return <div>BudgetSlider — Stage 9</div>;
}
