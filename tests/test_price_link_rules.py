# tests/test_price_link_rules.py
# Tests for validators/price_link_rules.py (Stage 8).
# Covers: stale price (>24h) blocked; fresh price passes; dead link blocked;
# live link passes; missing affiliate tag blocked; tag present passes.
# Stage 8: add tests. No live affiliate calls — use fixtures.
