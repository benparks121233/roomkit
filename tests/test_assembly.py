# tests/test_assembly.py
# Tests for services/assembly_service.py (Stage 9).
# Covers: board total equals sum of snapshot prices; render_url attached;
# board reads from snapshots not live data; missing render_url handled gracefully.
# Stage 9: add tests.
