# tests/test_snapshot.py
# Tests for services/snapshot_service.py (Stage 8).
# Covers: snapshot written with correct fields; snapshot is immutable after creation
# (re-running same input does not mutate existing snapshot);
# stored design reads from snapshot not live data.
# Stage 8: add tests.
