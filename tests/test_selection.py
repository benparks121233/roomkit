# tests/test_selection.py
# Tests for services/selection_service.py (Stage 7).
# Covers: best style-fit product chosen; null + "no_spec_match" if no candidate meets specs;
# null + "no_candidate" if candidate list empty; price band respected.
# Stage 7: add tests.
