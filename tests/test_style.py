# tests/test_style.py
# Tests for services/style_service.py (Stage 4).
# Covers: StyleProfile validates against schema; maps to a known style profile;
# low-confidence input sets fallback=True.
# Stage 4: add tests.
