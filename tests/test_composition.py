# tests/test_composition.py
# Tests for services/composition_service.py (Stage 5).
# Covers: total allocated budget never exceeds target; required slots present;
# LLM over-weight proposals are clamped by code.
# Stage 5: add tests.
