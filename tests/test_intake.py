# tests/test_intake.py
# Tests for services/intake_service.py (Stage 3).
# Covers: valid RoomRequest produced; missing fields are null not guessed;
# invalid budget (≤0) rejected; invalid room_type rejected.
# Stage 3: add tests.
